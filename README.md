# Mongo-Scrape

This is a server-side application that uses Cheerio to get the latest news from New York Times. The data is stored on MongoDB. It uses express to get and the articles from MongoDB database. This is built using Handlebars.
